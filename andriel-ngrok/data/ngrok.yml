# ngrok configuration file
# Documentation: https://ngrok.com/docs/agent/config/

version: 2

# Example tunnels - uncomment and modify as needed:

# tunnels:
#   # Expose a local web service
#   my-website:
#     proto: http
#     addr: 80
#
#   # Expose with custom subdomain (requires paid plan)
#   my-api:
#     proto: http
#     addr: 3000
#     # subdomain: my-custom-subdomain
#
#   # TCP tunnel for non-HTTP services
#   ssh:
#     proto: tcp
#     addr: 22

# Web interface settings
web_addr: 0.0.0.0:4040

# Logging
log_level: info
log_format: json
